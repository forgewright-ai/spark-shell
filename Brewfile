# spark-shell Brewfile -- the macOS package list. `spark-shell on` runs it;
# the Linux lists live in distro/*.env. The AI itself needs nothing from
# here. Homebrew has no pinning worth trusting: this records what, not
# which version.
brew "tmux"              # the terminal multiplexer the status line rides
brew "starship"          # the prompt (PROMPT=starship)
brew "bat"               # the shell's daily tools
brew "eza"
brew "fzf"
brew "zoxide"
brew "ripgrep"
brew "fd"
brew "jq"
brew "btop"
brew "yazi"              # the file manager
brew "ncurses"           # a tmux-256color terminfo with modified arrow keys
