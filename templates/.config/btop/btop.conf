#? rendered by spark install.sh from templates/.config/btop/btop.conf
#? btop rewrites this file on exit, so it is a regular file, not a symlink:
#? your in-app changes stick, and `install.sh` will offer to back them up.
color_theme = "@THEME_BTOP@"
theme_background = False
truecolor = True
vim_keys = True
update_ms = 2000
proc_sorting = "cpu lazy"
proc_tree = False
show_battery = True
